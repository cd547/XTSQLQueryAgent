{
  "table_name": "cms_apply",
  "field_aliases": {},
  "field_enums": {},
  "virtual_associations": [
    {
      "name": "关联报名用户",
      "target_table": "my_user",
      "join_condition": "my_user.id = cms_apply.my_user_id",
      "type": "many_to_one",
      "description": "多个报名记录可对应同一个C端用户",
      "fields_mapping": {
        "my_user_id": "id"
      },
      "business_rule": ""
    },
    {
      "name": "关联活动",
      "target_table": "cms_article",
      "join_condition": "cms_article.id = cms_apply.cms_article_id",
      "type": "many_to_one",
      "description": "多个报名记录可对应同一个活动",
      "fields_mapping": {
        "cms_article_id": "id"
      },
      "business_rule": ""
    }
  ],
  "calculated_fields": {},
  "business_constraints": {},
  "business_rules": [],
  "field_semantics": {
    "created_time": {
      "ddl_type": "VARCHAR",
      "actual_type": "timestamp_ms",
      "description": "DDL 声明 VARCHAR，但实际存的是 13 位毫秒时间戳",
      "parse_expression": "FROM_UNIXTIME(CAST({col} AS UNSIGNED) / 1000)",
      "common_uses": {
        "year_equal_current": "YEAR({parsed}) = YEAR(CURDATE())",
        "month_range": "{parsed} BETWEEN ? AND ?"
      }
    }
  }
}
