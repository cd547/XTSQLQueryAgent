# 变更日志 2026-08-25

> 说明：§A 文档核对修订；§B file_storage 路径修复；§C 前端模型显示修复；§D 需求登记；§E `/generate` catch 作用域修复；§F Agent 性能优化（tokenizer / RA prefix cache / 提示词去噪）；§G **移除折叠机制**（分批调用根因）。

## G. 移除折叠机制：多表 schema 分批调用的根因（后端 / llm.js + responsesApi.js + toolFuncs.js）

### 现象

用户实测（换模型、换 CC 均稳定复现）：问"今天的排课"→ `get_sliced_index([course_edu, course_school])` 返回的表卡片中明确列有 `edu_study 的关联表: edu_course_exam_subject_name, ...`，但续问后模型仍分两轮分别调 `get_table_schema(["edu_study"])`、`get_table_schema(["edu_course_exam_subject_name"])`，浪费一整轮 LLM 往返（~10s + 1096 completion tokens），违反 SKILL.md 规则 9"禁止分批"。

### 根因（代码层，非模型问题）

`compactConsumedToolResults` 将"已消费历史区"的 sliced 结果替换为 `formatTableInfoCompact` 卡片——**唯一差异就是删掉 related_tables 一行**（约束/规则展开均保留）。后果链：

1. 折叠边界 = 最后一个含 tool_calls 的 assistant → R1 之后每轮推进，sliced 结果落入折叠区；
2. user_choice 续问场景最严重：边界落在 user_choice assistant 上，**续问第 0 轮就看不到关联表**；
3. 模型只能从已获取表的 schema fk 字段"渐进式发现"关联表 → 逐轮补查；
4. 双重锁死：重新调 `get_sliced_index` 会被 dupCheck 拦截（域已加载）。

即：SKILL.md 要求批量规划所依据的信息，恰好被程序在决策时点删掉了——所以与模型无关、必然复现。

### 量化（方案 A vs B 对比后选 B）

```
完整版（不折叠）: 3627 字符 ≈ 2620 tokens
折叠版（原现状）: 1713 字符 ≈ 1649 tokens   节省仅 971 tokens
```

- 方案 A（Compact 加回关联表行）：加回后 Compact ≈ 完整版，节省归零，等于白留整套机制 → **A ≈ B**；
- 隐藏代价：折叠使历史中段字节突变 → prefix cache 从该点失效一次；不折叠则历史纯追加、cache 最稳；
- 收益：模型任意轮次可批量规划 → 典型多表问题省一整轮往返（远超 971 tokens 的缓存友好成本）。

### 改动

| 文件 | 内容 |
|---|---|
| `llm.js` | 删除 `compactConsumedToolResults`（134 行）+ `foldedCache`；`requestMessages` 直接由 `messages` 做 reasoning 剥离得到 |
| `responsesApi.js` | 同步移除调用与 foldedCache（RA 与 CC 一致） |
| `toolFuncs.js` | 删除 `formatTableInfoCompact`（唯一消费者是折叠机制）；`formatTableInfo` 成为唯一实现 |

### 验证

- 三文件 `node --check` 通过；全量 `node --test` **29/29 通过**（无测试引用被删函数）；
- 待用户重跑同一问题验证：预期续问 R0 即可一次传入 `["edu_study","edu_course_exam_subject_name", ...]` 批量拉取。

## F. 性能优化：Agent 执行时间与 token 消耗（后端 / llm.js + responsesApi.js + agentHelpers.js）

### 背景（效率分析结论，2026-08-25）

| 项 | 量化 | 用户决策 |
|---|---|---|
| `saveMessagesToDb` 每轮对全量上下文跑自研 BPE（同步阻塞事件循环） | 实测 36KB→655ms / 101KB→1732ms / 247KB→5004ms **每轮**；30 轮提问累计浪费十几秒 CPU，单进程下阻塞所有并发请求 | ✅ 采用方案 3 |
| RA 路径每轮末尾追加 checklistMsg → 字节不稳定 → prefix cache 每轮失效（全价 token） | 与 CC 的 F23 改造不一致 | ✅ 与 CC 对齐 |
| 拦截提示 `buildChecklist()` 输出已废弃的 `get_tables` 行 | 噪音信息诱导 LLM 调不存在的工具 | ✅ 移除 |
| toolFuncs `loadTableIndex` 实时读盘无缓存 | ~3ms/次 | ❌ 保持实时读盘（用户要求热更新即时生效）|
| 每轮全量 requestParams pretty-print 日志 | 磁盘膨胀 | ❌ 保留（开发期排障需要）|
| SKILL.md 规则2 允许 UPDATE/DELETE 与执行白名单"冲突" | — | ❌ 有意设计：允许生成、禁止执行 |

### F1. `message_tokens` 改用 API 权威 usage（llm.js + responsesApi.js + agentHelpers.js）
- `saveMessagesToDb` 新增第 4 参 `messageTokens`：传入则直接用（推荐本轮 `usage.prompt_tokens`），未传回落旧 BPE 计算；
- **CC 路径**：`runSqlAgent` 流式解析中记录 `latestPromptTokens`（来自 `usage.prompt_tokens`），两处 save 调用均传入；
- **RA 路径**（2026-08-25 补齐，用户指出首版漏改）：generator 从流事件中捕获 `usage.prompt_tokens`（Responses API 的 `input_tokens`）记入 `latestPromptTokens`；`agentHelpers.saveRunState` 新增可选 `messageTokens` 入参透传，两处 saveRunState 调用（每轮持久化 + user_choice 终止分支）均已接入；
- 口径变化：`llm_messages.message_tokens` 从"本地 BPE 全量累计"变为"最近一轮请求的 prompt_tokens"——前端"上下文长度"展示语义不变且更准（多模态数组内容旧算法本来就数不到）。CC/RA 双路径口径一致。

### F2. Responses 路径与 CC 对齐 get_call_history 机制（responsesApi.js + agentHelpers.js）
- 移除 RA 每轮末尾追加的 `checklistMsg`（prefix cache 杀手）；`buildToolCallChecklistMessage` import 一并清理；
- assistant push 前注入常量 id 的 `synthetic_get_call_history`（仅当有真实工具调用且未误调时）；
- `agentHelpers.executeToolCallsInStages`：`availableToolNames` 补入 `get_call_history` + 新增拦截器（返回基于 `reg.callHistory` 的稳定 JSON）；
- 新增 `recordExecResultsToCallHistory()` 导出（CC 同款登记逻辑），RA 在工具执行后调用；
- 补齐 gchLoopCount 双轮熔断（连续两轮仅调 get_call_history → 强制终止）。

### F3. 提示词去噪（llm.js）
- `buildChecklist()` / `buildToolCallChecklistMessage()` 移除 `get_tables` 条目（工具已停用，展示"未调用"只会误导 LLM）。

### 验证
- 三文件 `node --check` 通过；全量 `node --test` **29/29 通过**；
- tokenizer 基准数据存档于本节背景表（修复前基线）。

## E. 功能修复：`/generate` catch 块作用域错误吞掉真实异常（后端 / routes/query.js）

- 现象：任何穿透到路由层 catch 的流式异常（典型："LLM 未配置"、buildSystemMessage 读盘失败——二者均在 runSqlAgent 内层 try 之外），用户看到的错误是"生成失败：messageSaved is not defined"而非真实原因；catch 分支的中断 partial 落库 + session token 累计从未生效（死代码）。
- 根因：`fullContent/sql/message/allLogs/totalPromptTokens/totalCompletionTokens/totalTokens/messageSaved/lastRound/userChoiceRequestFromStream` 共 10 个变量全部声明在 try 块内，而下方 catch 块引用它们。JS 词法作用域规则：try 块内声明的 let/const 对 catch 块不可见。
- 触发路径澄清：用户主动 stop / 超时 / 断连**不走**此 catch——llm.js 内层 try 自行捕获 AbortError 后 yield error 并正常 return，路由的 for-await 正常结束、走 happy-path 兜底落库（这就是该 bug 长期未暴露的原因）。
- 修复：
  1. 10 个变量声明提升到 `try {` 之前（query.js:484-503），catch 引用回到同一函数作用域；
  2. 新增源码结构回归测试 `test/test-generate-catch-scope.mjs`（7 断言：声明在锚点前 / 锚点与 catch 间无重声明 / catch 内守卫与内容来源仍健在），防止未来重构改回。
- 验证：最小复现脚本确认原写法抛 ReferenceError；`node --check` 通过；全量 `node --test` **29/29 通过**。
- 登记：06-问题清单 E29。

## C. 功能修复：改模型后聊天页模型标签不更新（前端 / ConfigPanel.jsx + App.jsx）

- 现象：在配置面板切换模型并保存后，聊天输入框下方的模型名标签（`ChatInput.jsx:314`）仍是旧值，要刷新页面才更新。
- 根因：聊天页模型显示的数据源是 `App.jsx` 里 `useAppConfig` 的 `currentModel`，仅在应用挂载时通过 `loadCurrentModel()` 加载一次（App.jsx:257，有 `initialLoadRef` 守卫）；`ConfigPanel.saveLlm()` 保存成功后只刷新面板自身 state，无任何机制通知 App。
- 修复：
  1. `ConfigPanel.jsx` 新增两个可选回调 prop：`onLlmConfigSaved`（saveLlm 成功后触发）、`onAgentConfigSaved`（saveAgent 成功后触发——同类问题：token 警告阈值改后进度条阈值也是旧的）；
  2. `App.jsx` 渲染处传入 `onLlmConfigSaved={loadCurrentModel}`、`onAgentConfigSaved={loadAgentConfig}`；
  3. **顺带修复同文件真 bug**：`testDb` / `saveDb` 使用了未定义变量 `message[...]`（应为 `messageApi`），ReferenceError 被 catch 吞掉导致"测试连接"无论成败永远提示"连接失败"；改为 `messageApi` 并正确透传后端消息。
- 验证：`npm run build` 通过；手动场景——打开配置 → 切模型 → 保存 → 聊天页模型标签立即变为新值；Agent 阈值同理。

## B. 功能修复：file_storage 存绝对路径不可移植（后端 / files.js + sqlite.js + index.js）

- 现象：用户指出 `file_storage.file_path` 存的是**绝对路径**（如 `D:\...\backend\file_cache\file-api-xxx`）。项目挪目录 / Electron 便携模式换盘符 / 换机器部署后，全部索引失效，历史图片回显退化为 404。
- 根因：`writeStorage` 直接把 `join(FILE_CACHE_DIR, fileId)` 的绝对路径写进 DB。而缓存目录是扁平结构（`file_cache/<file_id>`），路径完全可由 file_id 推导——DB 根本不需要存绝对路径。
- 修复（A10 v3）：
  1. `services/files.js` 新增 `resolveStoragePath(filePath)`：相对文件名 → `join(FILE_CACHE_DIR, file_path)`；兼容历史绝对路径行（原样使用）；
  2. `writeStorage` 改存**相对文件名（=file_id）**；
  3. `readStorage` / `removeStorage` 统一走 `resolveStoragePath` 解析；
  4. 新增 `migrateFileStorageRelativePaths()`：启动时幂等批量把存量绝对路径行规范化为文件名（事务 + log 记录迁移条数；失败仅 warn 不阻塞启动）；
  5. `index.js` 启动序列在 `ensureFileCacheDir()` 后调用迁移；`db/sqlite.js` schema 注释同步。
- 兼容性：迁移前老行仍可读（resolveStoragePath 兼容绝对路径），迁移后与磁盘实际位置解耦。
- 测试：新增 `test/test-file-storage-relative-path.mjs`（7 断言：绝对路径规范化 / 相对行不动 / 幂等 / 空表 no-op）；全量 `node --test` **29/29 通过**。
- **清理（同日，用户启动验证后）**：
  - 实测启动一次后 `file_storage` 2 行全部变为相对文件名（`[REL] file-api-f0fb8504-...` / `file-api-a7f940c7-...`），磁盘文件 + meta 齐全；
  - 按约定删除一次性迁移功能：`files.js` 删 `migrateFileStorageRelativePaths()` + `resolveStoragePath` 去掉绝对路径兼容分支（简化为纯 `join`）、移除 `basename/isAbsolute` import；`index.js` 删调用与 import；删除测试 `test/test-file-storage-relative-path.mjs`；`sqlite.js` schema 注释同步；
  - `files.js` 保留历史注记注释说明迁移已发生并删除，防止后人困惑"为什么没有迁移逻辑"；
  - 全量回归 `node --test` **28/28 通过**。
- **v4：彻底去掉冗余 file_path 列（用户指出 file_id 与 file_path 数据完全相同）**：
  - `db/sqlite.js`：新 schema 仅 `file_id PK + mimetype/bytes/saved_at`；启动时检测老表含 `file_path` 列 → 事务内建新表拷数据 → 原子替换（数据保留，回显不断档）；顺带修正古老 BLOB 迁移的顺序问题（原代码 DROP 后并未真正重建，现移到 CREATE 之前）；
  - `services/files.js`：read/write/remove 全部不再触碰路径字段，磁盘路径统一 `join(FILE_CACHE_DIR, fileId)` 推导；删除 `resolveStoragePath()`；
  - 新增回归测试 `test/test-file-storage-schema.mjs`（9 断言：老表重建保数据 / 幂等 / 古老 BLOB 表 DROP——子进程隔离验证）；全量 `node --test` **29/29 通过**；
  - 存量真实库（2 行附件元数据）在下次后端启动时自动完成重建。
- **v5：删除一次性 schema 迁移代码（用户确认真实库已升级后）**：
  - 实测启动后真实库 `file_storage` 列为 `file_id/mimetype/bytes/saved_at`（file_path 已移除，2 行数据完整保留，bytes 与磁盘文件一致）；
  - 按约定删除迁移②（file_path 列重建）与迁移①（古老 BLOB 表 DROP）：`db/sqlite.js` 仅保留最终 schema 的 `CREATE TABLE IF NOT EXISTS` + 索引；删除测试 `test/test-file-storage-schema.mjs`；全量回归 **28/28 通过**；
  - ⚠️ 若其他机器仍有**极老库副本**（含 `file_path` 或 BLOB `content` 列），新代码写入会因表结构不匹配报错，手工修复 SQL：
    ```sql
    -- 老表含 file_path / content 列时执行：
    CREATE TABLE file_storage_new (
      file_id TEXT PRIMARY KEY,
      mimetype TEXT,
      bytes INTEGER,
      saved_at DATETIME DEFAULT CURRENT_TIMESTAMP
    );
    INSERT INTO file_storage_new (file_id, mimetype, bytes, saved_at)
      SELECT file_id, mimetype, bytes, saved_at FROM file_storage;  -- BLOB 版无这些列时可跳过此行
    DROP TABLE file_storage;
    ALTER TABLE file_storage_new RENAME TO file_storage;
    CREATE INDEX IF NOT EXISTS idx_file_storage_saved_at ON file_storage(saved_at);
    ```

## A. 文档核对修订（纯文档，代码零改动）

## 一、背景：文档 vs 代码核对结论

| # | 文档位置 | 文档原说法 | 代码实际 | 处理 |
|---|---|---|---|---|
| 1 | `01 §3.3` / `05 §5.3`、§6.3 | 工具集"当前 6 个"，含 `get_domain_index` | `toolFuncs.js:1009` `LLM_TOOLS` 已过滤 `get_domain_index` + `get_call_history`，**LLM 可见仅 5 个**；域清单已内嵌 system 消息 | ✅ 已修 |
| 2 | `01 §3.4` / `03 §13.4` / `06 E28` | 发送时附带全部 `uploadedFiles.map(f=>f.id)`；"模型兼容性不做处理" | A4 改造后走 `selectedFileIds` **勾选子集**（上传后自动勾选 → 可 toggle → 发送成功 meta 后 `clearSelected()`）；非 vision 模型红条 + 二次确认 + 后端静默丢 | ✅ 已修 |
| 3 | `03 §13.6` 设计点 2 | 白名单 `/^file-api-[A-Za-z0-9]+$/`（无 `-`） | `query.js:311` 实为 `/^file-api-[A-Za-z0-9-]+$/`（含 `-`，file_id 形如 `file-api-dc6f9a94-8a3a-...`）；同文档 §13.3.6 原本就对，属内部矛盾 | ✅ 已修 |
| 4 | `03 §13.6` 设计点 5 | "paperclip 每次只取 1 个文件，不支持批量" | `ChatInput.jsx:121` `handleFileChange` 循环逐个调用，**支持多文件批量上传** | ✅ 已修 |
| 5 | `02 §5` | SQLite 数据模型无 `file_storage` 表 | A10 新增 `file_storage` 路径索引表（file_id → file_path/mimetype/bytes/saved_at，**不存 BLOB**），二进制落盘 `backend/file_cache/`；旧 BLOB schema 启动时自动 DROP 迁移（sqlite.js:250） | ✅ 已补 |
| 6 | `04-API参考.md` | 无 `/api/files` 组、`GET/PUT /config/files`、`GET /config/llm/balance` | 以上端点均存在（files.js / config.js:175 / config.js:240,248） | ⏳ 待补（本次未动 04，见待办） |
| 7 | `README §四` / `02 §4` | App.jsx 约 2497 行 | 拆分后实际 **1655 行**（06 问题清单 A14 已记 ~1660，README/02 未同步） | ✅ README 已修 |
| 8 | `README §四` / `02 §6` | 表索引 123 / ddl 121 / field_config 120 / 域文件 11 / 注册域 10 | **135** 张表（用户已手工去重 order_student/edu_student 重复条目）/ ddl **134** / field_config **133** / 域文件 **12** / 注册域 **11** | ✅ 已修 |
| 9 | `README §四` | 30 个测试脚本 | backend/test 实际 **28** 个 .mjs（changelog 验证节亦写"全量 28 测试"） | ✅ 已修 |

## 二、文档更新明细

- `01-功能说明.md`：§3.3 工具集改为"LLM 可见当前 5 个"+ 新增"已从 LLM 工具清单移除的定义"行（get_domain_index / get_call_history 过滤说明）；§3.4 "携带到消息"改为 selectedFileIds 勾选子集语义、"模型兼容性"改为 vision 守卫（前端红条+二次确认 / 后端静默丢）、新增"图片回显"行（本地缓存链路）；头部核对日期更新。
- `02-架构设计.md`：§3.2 路由表补 `/api/files` 组；§3.3 服务层补 `services/files.js` 与 `services/vision.js`；§4 前端模块表整体刷新（App.jsx 行数、ChatPanel/Sider/ChatInput/hooks/utils/constants 补全）；§5 ER 图 + 表清单补 `file_storage`（路径索引，不存 BLOB）+ 迁移策略注记；§6 Skill 资产计数刷新（135/134/133/12/11）；头部核对日期更新。
- `03-核心流程.md`：§13.4.2 hook 状态补 `selectedFileIds` / `blobUrlMapRef`；§13.4.4 改为勾选子集透传 + clearSelected 时机 + vision 守卫；§13.5 时序图同步；§13.6 设计点 2 正则含短横线、设计点 5 改多文件批量、设计点 7 改 vision 守卫、新增设计点 9（回显本地缓存链路 A5-A10 结论：DeepSeek user_data content 端点永远 404）；头部核对日期更新。
- `05-操作手册.md`：§5.3 注册域改为 11 个（course 拆为 course_edu/course_school）+ 注明域清单内嵌 system 消息；§6.3 故障排查"表查不到"行去掉 `get_domain_index` 引用；头部核对日期更新。
- `README.md`：§四 状态速览全面刷新（10 组路由 / App.jsx 1655 行 / Skill 计数 / 测试 28 个 / 补附件能力一行）。

## 三、遗留待办

- `04-API参考.md` 仍未收录：`POST /api/files/upload`、`GET /api/files`、`DELETE /api/files/:id`、`GET /api/files/:id/content`、`GET /api/files/_diagnose/:id`、`GET/PUT /api/config/files`、`GET /api/config/llm/balance`；`POST /generate` 请求体缺 `reasoning{enabled,effort}` 与 `fileIds` 字段说明。下次触碰 API 文档时一并补齐。
- `06-问题清单.md` E28 描述的是历史修复快照（当时确为 uploadedFiles 全量），按"已修复记录不改写历史"原则保留原文；现状以 `01 §3.4` 为准。

## 四、验证

- `table_index.json` 重数：`Select-String '"name": "'` 计 135 条，Group-Object 校验**无重复表名**（用户当日已手工去除 order_student ×2、edu_student ×2 重复条目）；
- `ddl/`=134、`field_config/`=133、`domains/`=12 个文件、`domain_router_index.json` 注册 11 域（Get-ChildItem / 手工清点）；
- `toolFuncs.js:1009-1010`：`LLM_TOOLS = tools.filter(t => t.name !== "get_domain_index" && t.name !== "get_call_history")`；
- `useFileUpload.js:153`：上传成功即 `next.add(normalized.id)` 自动勾选；`App.jsx:703-707`：payload 仅传 `[...selectedFileIds]`；
- `query.js:311`：白名单正则 `/^file-api-[A-Za-z0-9-]+$/`；
- `sqlite.js:235-257`：`file_storage` 建表 + BLOB schema DROP 迁移；`index.js:90` 启动序列 `ensureFileCacheDir()`。

## D. 需求登记（未实施）

### R1. per-user 模型选择（2026-08-25，用户提出，待排期）

- 需求：允许普通用户在共享 Key 前提下选择自己的默认模型（现状为全局 `llm_config` 单行，管理员改模型全体用户立即生效）。
- 已登记至 [06-问题清单.md A22](../06-问题清单.md)，含改动草案（users 表加列 / 入口按用户覆盖 / 模型列表权限 / 前端展示）。
- 状态：**待排期**，本次不动代码。
